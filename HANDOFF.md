# ХАРИС — Handoff для новой сессии

> Текущее состояние: **Фаза 3 завершена**. Работающая играбельная арена с обороной базы, врагами по волнам, стрельбой, HP игрока и базы. Тема — киберпанк (будет меняться на шумерскую).

---

## 1. Стек и запуск

- Three.js r158 + Vite 5, чистый JS (без фреймворков/TS)
- `npm run dev` → http://localhost:5173, `npm run build` → dist/
- Бандл: 136 KB gzip, 31 модуль

---

## 2. Структура файлов

```
src/
├── main.js              # Оркестратор: 270 строк, игровой цикл, HUD,  Death/Restart
├── config.js            # Все данные (без мёртвых секций)
├── core/
│   ├── SceneManager.js  # WebGLRenderer/PerspectiveCamera/ресайз/disposables
│   └── LightManager.js  # ambient/hemisphere/directional/spot/point + пульсация
├── world/
│   ├── StationBuilder.js    # Арена: пол, стены, потолок, контейнеры, неон
│   ├── ProceduralTextures.js # CanvasTexture: плиты пола, панели стен, hazard
│   └── Gates.js             # Трое ворот на севере, точки спавна, flash при спавне
├── entities/
│   ├── Player.js        # Капсула+визор+кольцо базы, HP 3, неуязвимость 1с
│   ├── Enemy.js         # FSM 6 состояний, 4 типа, steering seek/strafe
│   ├── Base.js          # Ядро базы: платформа+столб+сердце+кольца, HP 10
│   └── (Dummies.js удалён — заменён Enemies)
├── systems/
│   ├── Enemies.js       # Пул 24 врагов, shared геометрии, syncTargets для пуль
│   ├── Projectiles.js   # Пул InstancedMesh (32/48), цвет/скорость/дальность из конфига
│   ├── Waves.js         # 5 волн + endless, break/spawning/fighting, баннеры на РУС
│   ├── Input.js         # WASD/стрелки + тач-стик (левая половина экрана)
│   └── FollowCamera.js  # Игрок в нижней трети портрета, look-ahead
└── debug/
    └── DebugHelpers.js  # GridHelper/AxesHelper/ShadowCameraHelper
```

---

## 3. Архитектурные принципы (соблюдать)

1. **Config — единственный источник данных.** Каждая секция `config.js` читается кодом. Мёртвых секций нет.
2. **Shared ресурсы.** Материалы/геометрии создаются один раз, переиспользуются. Пул врагов/пуль — ноль аллокаций в кадре.
3. **FSM для врагов.** Состояния: `spawn→chase→windup→attack→recover→die`. Каждое состояние — один `case` в `Enemy.update()`.
4. **Steering отдельно от решения.** Враг решает куда идти (цель: база или игрок), `seek/strafe` — как двигаться.
5. **Телеграфы перед атакой.** `windup` 0.4с с усилением глаза — игрок читает удар.
6. **Детерминизм.** Раскладка контейнеров — фикс-массив, не `Math.random()` в билде.
7. **Портрет 9:16.** `#game` зафиксирован через `aspect-ratio`, камера берёт размер от контейнера.
8. **HMR-утилизация.** `import.meta.hot.dispose(() => game.dispose())` — иначе stale-кадр поверх нового.

---

## 4. Что работает (проверено симуляцией в браузере)

- ✅ Волны 1→6 (5 по докам + endless), состав растёт, баннеры на русском
- ✅ 58 фрагов за 2 мин, танки взрываются, мини-босс на 3-й волне
- ✅ Бегун идёт на базу (z: -17 → 13.9), бьёт игрока мимоходом
- ✅ Стрелок держит дистанцию 10–14м, стреляет с телеграфом
- ✅ HP игрока 3 + неуязвимость 1с + красная вспышка + сердца в HUD
- ✅ HP базы 10 + полоса сверху; база пала → «БАЗА УНИЧТОЖЕНА» + рестарт
- ✅ Спавн только из ворот (gateSpawnOk: true), вспышка при спавне
- ✅ Смерть игрока → рестарт волны, игрок в центр
- ✅ Сборка чистая, бандл 136 KB gzip

---

## 5. Известные оговорки

- В headless-браузере кадры троттлятся — боёвку проверял пошаговой симуляцией (240 шагов = 4с), не wall-clock.
- Красный неон боковых стен в кроп скриншота не попал, но геометрия та же что у циана.
- Автоприцел работает только при движении; без движения — разворот к ближайшей цели.

---

## 6. Ближайшая задача: Шумерский сеттинг + 20 уровней

### 6.1. Смена темы (киберпанк → шумер)

**Акт 1: Глина и тростник (уровни 1–7, обучение)**
- Палитра: песок `#c2a878`, тростник `#7a8a3a`, глина `#a0522d`, лазурит `#1e6091`
- Арена: песчаная плоскость вместо металлического пола, тростниковые борта
- Ворота: каменная арка с клинописью вместо неона
- База: зиккурат-постамент с факелом вместо циан-сердца
- Свет: тёплый солнечный, лёгкий туман пыли
- Новый враг: **Голем** (бегун) — коробка + выступы + каменный глаз

**Акт 2: Храм (уровни 8–14)**
- Палитра: обсидиан `#1a1a2e`, золото `#d4a843`, лазурит, сумерки
- Арена: каменные плиты, колонны по углам, зиккурат на фоне
- Ворота: портал с рельефами
- База: алтарь с вечным огнём
- Свет: сумеречный, факелы по углам
- Новый враг: **Пращник** (стрелок) — каменная сфера + глаз-рубин

**Акт 3: Эабису — подземелье (уровни 15–20)**
- Палитра: обсидиан, тёмно-синий `#0a0a1a`, факельное оранжевое `#ff6a2a`
- Арена: обсидиановый пол, сталактиты, узкие проходы
- Ворота: расщелины с красным свечением
- База: кристалл-ядро в расщелине
- Свет: почти темно, только факелы и свечение кристаллов
- Новый враг: **Шеду-бык** (танк) — массивный, рога, взрыв обломками

### 6.2. Стрелы вместо болтов

- Вытянутый снаряд (BoxGeometry 0.06 × 0.06 × 0.6) + оперение из двух треугольников
- Лёгкий след (Line или короткий trail из точек)
- Цвет: дерево/перо для игрока, обсидиан/сера для врагов
- Параметры в config: `combat.arrowSpeed`, `combat.arrowLife`

### 6.3. Каменные враги (те же FSM-роли, новые тела)

- **Голем (бегун):** коробка 0.8×1.2×0.8 + плечи-плиты + каменный глаз (эмиссив)
- **Пращник (стрелок):** восьмигранник + глаз-рубин + стойка-праща
- **Шеду-бык (танк):** массивный параллелепипед + рога (конусы) + ноги-цилиндры
- **Мини-босс:** увеличенный голем с золотыми вкраплениями

### 6.4. Генератор уровней

- `LevelDef` — данные: сид, тема, раскладка ворот (2–4), позиция базы, состав волн, модификаторы
- `mulberry32(сид)` — детерминированный RNG
- Модификаторы: быстрые враги, туман, темнота, extra-ворота, броня, двойной спавн
- 20 уровней = 3 акта × 7/7/6, каждый с уникальным сидом и темой

### 6.5. Порядок работы

1. **Theme.js** — палитры + наборы пропсов для 3 актов
2. **Перекрасить StationBuilder** — принимает `theme`, меняет материалы/текстуры
3. **Перекрасить Gates** — каменная арка, клинопись, цвет из темы
4. **Перекрасить Base** — зиккурат/алтарь/кристалл по теме
5. **Стрелы** — новая геометрия + оперение + след
6. **Голем** — новый тип тела в Enemy (runner-роль)
7. **LevelDirector** — сборка уровня из LevelDef + темы
8. **Проверка** — 20 уровней генерируются, первый играбелен

---

## 7. Чего НЕ делать

- Не добавлять мёртвые секции в config — каждую читать кодом или удалить
- Не аллоцировать в кадре — пулы, shared, `syncTargets()` кеширует массив
- Не менять FSM-каркас врага — только тела и параметры
- Не ломать портрет 9:16 — тестировать через `aspect-ratio` контейнера
- Не ставить `Math.random()` в билд-функции — только в update/спавн

---

## 8. Ключевые файлы для старта новой сессии

- `src/main.js` — игровой цикл, точка входа для новых систем
- `src/config.js` — добавлять секции `theme`, `levels`, `arrows`
- `src/entities/Enemy.js` — добавить тела голема/пращника/шеду
- `src/world/StationBuilder.js` — принимает `theme`, меняет материалы
- `src/systems/Waves.js` — уже работает, не ломать

---

*Дата: 2026-09-17. Сессия: фаза 3 завершена, следующая — шумерская тема + 20 уровней.*
